/**
 * Action Dispatcher Module Entry Point
 * 
 * Re-exports all action dispatcher functionality for backward compatibility.
 * This module is responsible for dispatching workflow actions to their appropriate
 * handlers, managing action registration, template processing, and execution.
 * 
 * @module action-dispatcher
 */

// Re-export from action-dispatcher module for backward compatibility
export * from "./action-dispatcher/index";
